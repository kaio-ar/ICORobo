public void run() {
    setColors(Color.pink, Color.blue, Color.white);

    while (true) {
        if (movingForward) {
            setAhead(150);
        } else {
            setBack(150);
        }
        execute();
    }
}
